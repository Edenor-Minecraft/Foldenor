From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: AltronMaxX <max06112004@gmail.com>
Date: Mon, 12 Jun 2023 18:26:42 +0400
Subject: [PATCH] Remove-isOwnedByCurrentRegion-check


diff --git a/src/main/java/io/papermc/paper/util/TickThread.java b/src/main/java/io/papermc/paper/util/TickThread.java
index 1437be5ea54de6359a552d6db2e00bb61e8e590d..d6aac4898b8d4ff6f8a9a489ae532655af2d545c 100644
--- a/src/main/java/io/papermc/paper/util/TickThread.java
+++ b/src/main/java/io/papermc/paper/util/TickThread.java
@@ -145,15 +145,10 @@ public class TickThread extends Thread {
         }
         if (region == null) {
             try{
-                if (!Bukkit.isOwnedByCurrentRegion(world.getWorld(), chunkX, chunkZ)){
-                    region = world.regioniser.getRegionAtSynchronised(chunkX, chunkZ);
-                    if (region == null){
-                        MinecraftServer.LOGGER.warn("Region at position [" + chunkX + ", " + chunkZ + "] is null!");
-                        return isShutdownThread();
-                    }
-                }
-                else{
-                    return false;
+                region = world.regioniser.getRegionAtSynchronised(chunkX, chunkZ);
+                if (region == null){
+                    MinecraftServer.LOGGER.warn("Region at position [" + chunkX + ", " + chunkZ + "] is null!");
+                    return isShutdownThread();
                 }
             } catch (Exception e) {
                 MinecraftServer.LOGGER.warn("Region at position [" + chunkX + ", " + chunkZ + "] is null!");
@@ -197,15 +192,10 @@ public class TickThread extends Thread {
             TickRegionScheduler.getCurrentRegion();
         if (region == null) {
             try{
-                if (!Bukkit.isOwnedByCurrentRegion(world.getWorld(), fromChunkX, fromChunkZ)){
-                    region = world.regioniser.getRegionAtSynchronised(fromChunkX, fromChunkZ);
-                    if (region == null){
-                        MinecraftServer.LOGGER.warn("Region at position [" + fromChunkX + ", " + fromChunkZ + "], [" + toChunkX + ", " + toChunkZ + "] is null!");
-                        return isShutdownThread();
-                    }
-                }
-                else{
-                    return false;
+                region = world.regioniser.getRegionAtSynchronised(fromChunkX, fromChunkZ);
+                if (region == null){
+                    MinecraftServer.LOGGER.warn("Region at position [" + fromChunkX + ", " + fromChunkZ + "], [" + toChunkX + ", " + toChunkZ + "] is null!");
+                    return isShutdownThread();
                 }
             } catch (Exception e) {
                 MinecraftServer.LOGGER.warn("Region at position [" + fromChunkX + ", " + fromChunkZ + "], [" + toChunkX + ", " + toChunkZ + "] is null!");
